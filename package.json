{
  "name": "agent-watch",
  "version": "0.1.0",
  "description": "Local activity and stall monitor for Codex and Claude Code",
  "type": "module",
  "private": false,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js --open",
    "start:no-open": "node src/server.js",
    "check": "node --check src/server.js && node --check src/monitor.js && node --check public/app.js && node --check public/mini.js"
  },
  "keywords": [
    "codex",
    "claude-code",
    "monitor",
    "privacy",
    "developer-tools"
  ]
}
